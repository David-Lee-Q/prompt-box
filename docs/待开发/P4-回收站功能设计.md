# P4：回收站功能设计

> 日期: 2026-06-13 | 状态: 二轮评审修正

> **二轮修正**：trashPrompt 幂等检查、savePrompt 自动恢复、emptyTrash 批事务、restoreScene 独立删除处理、Toast 增强需求、测试影响评估

---

## 一、目标

提示词和场景的删除操作改为软删除：先进入回收站，30 天后自动清理。恢复操作撤销软删除。

**评审修正要点：** 同步上线场景+提示词回收站（非分阶段）；补充遗漏查询点；增加撤销 Toast；导入时剥离 `deletedAt`。

---

## 二、数据模型

### 2.1 软删除字段

```ts
// types/index.ts
interface Scene {
  // ... existing fields
  deletedAt?: number;  // null = active, timestamp = in trash
}

interface Prompt {
  // ... existing fields  
  deletedAt?: number;
}

// Version 不单独标记——跟随其 prompt。prompt 恢复时版本自动恢复。
// 硬删除 prompt 时级联硬删除其所有版本。
```

### 2.2 Dexie v4 升级

```ts
// db/index.ts
db.version(4).stores({
  scenes: 'id, name, sortOrder, createdAt, deletedAt',
  prompts: 'id, sceneId, name, *tags, isStarred, createdAt, updatedAt, deletedAt',
  versions: 'id, promptId, version, score, createdAt',
});
```

旧数据无 `deletedAt`（`undefined == null` → true），天然兼容，无需迁移脚本。

### 2.3 辅助函数（防遗漏）

```ts
// src/services/helpers.ts
export function activeOnly<T extends { deletedAt?: number }>(items: T[]): T[] {
  return items.filter(item => item.deletedAt == null);
}
```

所有查询点统一使用 `activeOnly()` 而非内联 filter，降低遗漏风险。

---

## 三、完整查询审计

### 必须加 `deletedAt` 过滤的查询点

| 文件 | 函数 | 当前行为 | 修复 |
|------|------|----------|------|
| `promptService.ts` | `getPromptsByScene()` | 返回场景下所有 prompt | `activeOnly()` |
| `promptService.ts` | `getStarredPrompts()` | 返回所有收藏 | `activeOnly()` |
| `promptService.ts` | `getAllPrompts()` | 返回所有 | `activeOnly()` |
| `promptService.ts` | `searchPrompts()` | 搜索所有 | `activeOnly()` |
| `promptService.ts` | `getPromptsByTag()` | 按标签搜索 | `activeOnly()` |
| `promptService.ts` | `getAllTags()` | 构建标签集合 | `activeOnly()` — **设计遗漏点** |
| `sceneService.ts` | `getScenes()` | 返回所有场景 | `activeOnly()` |
| `Sidebar.tsx` | promptCount（每场景） | 全量计数 | `activeOnly()` — **设计遗漏点** |
| `StatusBar.tsx` | `db.prompts.count()` | 全量计数 | `db.prompts.filter(p => !p.deletedAt).count()` |
| `export-import.ts` | `exportAllData()` | 全量导出 | `activeOnly()` — **设计遗漏点** |
| `export-import.ts` | `exportScene()` | 场景下全量 | `activeOnly()` — **设计遗漏点** |

### 不加过滤的查询点

| 文件 | 函数 | 原因 |
|------|------|------|
| `promptService.ts` | `getPrompt(id)` | 回收站页需读取已删除项详情 |
| `sceneService.ts` | `getScene(id)` | 同上 |
| `promptService.ts` | `savePrompt()` | 需显式检查 scene.deletedAt（见 4.1） |

---

## 四、服务层

### 4.1 promptService.ts 新增/修改

```ts
// 查询辅助
const activePrompts = () => db.prompts.filter(p => p.deletedAt == null);

// 所有列表查询改用 activePrompts() 或 activeOnly()

// 新增
export async function trashPrompt(id: string): Promise<void> {
  const prompt = await db.prompts.get(id);
  if (!prompt || prompt.deletedAt != null) return; // ⚠️ 幂等：已在回收站则 no-op
  await db.prompts.update(id, { deletedAt: Date.now() });
}

export async function restorePrompt(id: string): Promise<void> {
  const prompt = await db.prompts.get(id);
  if (!prompt) return;
  
  // ⚠️ 检查场景状态：如果场景在回收站，弹出对话框让用户选目标场景
  // 由调用方（UI层）处理，Service 只负责数据操作
  await db.prompts.update(id, { deletedAt: undefined });
}

export async function permanentDeletePrompt(id: string): Promise<void> {
  await db.transaction('rw', db.prompts, db.versions, async () => {
    await db.versions.where('promptId').equals(id).delete();
    await db.prompts.delete(id);
  });
}

export async function getTrashPrompts(): Promise<Prompt[]> {
  return db.prompts.filter(p => p.deletedAt != null).toArray();
}

export async function emptyTrash(cutoff?: number): Promise<void> {
  const trash = await getTrashPrompts();
  const toDelete = cutoff 
    ? trash.filter(p => p.deletedAt! < cutoff)
    : trash;
  // ⚠️ 单事务批处理，避免 N+1 事务开销
  await db.transaction('rw', db.prompts, db.versions, async () => {
    for (const p of toDelete) {
      await db.versions.where('promptId').equals(p.id).delete();
      await db.prompts.delete(p.id);
    }
  });
}

export async function getTrashCount(): Promise<number> {
  return db.prompts.filter(p => p.deletedAt != null).count();
}
```

### 4.2 sceneService.ts 新增/修改

```ts
export async function trashScene(id: string): Promise<void> {
  await db.transaction('rw', db.scenes, db.prompts, async () => {
    await db.scenes.update(id, { deletedAt: Date.now() });
    // ⚠️ 只级联标记当前活跃的 prompt（已在回收站的不重复标记）
    const prompts = await db.prompts.where('sceneId').equals(id)
      .filter(p => p.deletedAt == null).toArray();
    const now = Date.now();
    for (const p of prompts) {
      await db.prompts.update(p.id, { deletedAt: now });
    }
  });
}

export async function restoreScene(id: string): Promise<void> {
  await db.transaction('rw', db.scenes, db.prompts, async () => {
    await db.scenes.update(id, { deletedAt: undefined });
    // ⚠️ 只恢复由本场景级联删除的 prompt（同一时间戳，或直接从 active→trash 的）
    // 最简实现：恢复场景下所有 prompt（用户主动恢复场景 = 意图恢复全部）
    // 如果未来有冲突场景，可改为确认对话框
    const prompts = await db.prompts.where('sceneId').equals(id).toArray();
    for (const p of prompts) {
      await db.prompts.update(p.id, { deletedAt: undefined });
    }
  });
}

export async function permanentDeleteScene(id: string): Promise<void> {
  await db.transaction('rw', db.scenes, db.prompts, db.versions, async () => {
    const prompts = await db.prompts.where('sceneId').equals(id).toArray();
    for (const p of prompts) {
      await db.versions.where('promptId').equals(p.id).delete();
      await db.prompts.delete(p.id);
    }
    await db.scenes.delete(id);
  });
}
```

### 4.3 savePrompt 场景校验

```ts
// promptService.ts savePrompt()
const scene = await db.scenes.get(prompt.sceneId);
if (!scene || scene.deletedAt != null) throw new Error('场景不存在');
// ⚠️ 新增 scene.deletedAt 检查 + 自动恢复已删除项

// savePrompt() update 分支，在写入前：
if (existingPrompt.deletedAt != null) {
  // 编辑已删除项 → 自动恢复
  // 后续 update 中显式加 deletedAt: undefined
}

// ⚠️ 新增 scene.deletedAt 检查，防止 prompt 保存到已删除场景
```

### 4.4 importData 剥离 deletedAt

```ts
// export-import.ts importData()
for (const scene of writeScenes) delete (scene as any).deletedAt;
for (const prompt of writePrompts) delete (prompt as any).deletedAt;
// ⚠️ 导入是主动行为，强制清除删除标记，数据以活跃状态出现
```

---

## 五、UI 设计

### 5.1 删除确认 + 撤销 Toast

```
删除时弹出 5 秒撤销 toast：

┌──────────────────────────────────┐
│  ✓ 已移入回收站    [撤销]        │
└──────────────────────────────────┘

点击「撤销」→ 立即恢复，toast 消失。
5 秒后自动消失，不恢复。
```

### 5.2 回收站入口

侧边栏底部：

```
┌──────────┐
│  回收站 🗑 │ 3  ← badge 显示数量
└──────────┘
```

### 5.3 回收站页面（`#/trash`）

```
┌─────────────────────────────────────┐
│  回收站                               │
│  已删除项将在 30 天后自动清理            │
├─────────────────────────────────────┤
│                                     │
│  空状态: 🗑 回收站为空                 │
│                                     │
│  📁 营销文案 (场景)                    │
│     删除于 3 天前    [恢复] [永久删除]  │
│                                     │
│  📝 代码审查助手                       │
│     删除于 1 天前    [恢复] [永久删除]  │
│                                     │
├─────────────────────────────────────┤
│  [清空回收站]                          │
└─────────────────────────────────────┘
```

### 5.4 永久删除确认（防误操作）

```
┌────────────────────────────┐
│  ⚠️ 永久删除                 │
│                            │
│  此操作不可撤销。请输入        │
│  "代码审查助手" 确认：        │
│  [_____________________]   │
│                            │
│  [取消]      [确认删除]      │
└────────────────────────────┘
```

### 5.5 恢复 prompt 而其场景在回收站时

```
┌──────────────────────────────────────┐
│  该提示词的场景 "营销文案" 也在回收站中   │
│                                      │
│  请选择恢复后的目标场景：                │
│  [未分类（自动创建）▾]                  │
│                                      │
│  [取消]                  [恢复]       │
└──────────────────────────────────────┘
```

### 5.6 UI 状态覆盖

| 状态 | 处理 |
|------|------|
| 回收站为空 | 🗑 图标 + "回收站为空" 文案 |
| 恢复成功 | Toast "已恢复" |
| 永久删除 loading | Button disabled + spinner |
| 自动清理通知 | 下次打开扩展时 Toast "已自动清理 X 条过期项" |

---

## 六、交互规则

| 场景 | 行为 |
|------|------|
| 删除场景 | 场景 + 其下**活跃** prompt 同时标记 `deletedAt` |
| 恢复场景 | 场景 + 其下所有 prompt 清除 `deletedAt` |
| 单独删除 prompt | 仅该 prompt 标记 |
| 先后删 prompt 再删 scene → 恢复 scene | prompt 被恢复（用户恢复 scene = 意图恢复全部） |
| 恢复 prompt 但其 scene 也在回收站 | **弹窗选择目标场景**（默认创建"未分类"场景） |
| 编辑已删除 prompt | 自动恢复（清除 `deletedAt`） |
| 重复软删除 | No-op（不更新时间戳，防止绕过 30 天限制） |
| 搜索/筛选 | 不搜回收站内容 |
| 导出 | 不含已删除项 |
| 导入 | 强制清除 `deletedAt`，以活跃状态出现 |
| 快照 | 含已删除项（用于灾难恢复），恢复时保持原状态 |
| 清空回收站 | 永久删除所有软删除项 |
| 自动清理 | 下次打开扩展时清理 >30 天的旧项 |

---

## 七、Store 结构

```ts
// useAppStore.ts 新增
interface AppStore {
  // 新增
  trashCount: number;
  
  loadTrashCount: () => Promise<void>;
  trashPrompt: (id: string) => Promise<void>;
  trashScene: (id: string) => Promise<void>;
  restorePrompt: (id: string) => Promise<void>;
  restoreScene: (id: string) => Promise<void>;
  permanentDeletePrompt: (id: string) => Promise<void>;
  permanentDeleteScene: (id: string) => Promise<void>;
  emptyTrash: () => Promise<void>;
}
```

TrashPage 直接调用 service 层获取数据，不缓存到全局 store。

---

## 八、文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/types/index.ts` | 修改 | Scene + Prompt 增加 `deletedAt?: number` |
| `src/db/index.ts` | 修改 | Dexie v4 schema |
| `src/services/helpers.ts` | 修改 | 新增 `activeOnly()` |
| `src/services/promptService.ts` | 修改 | 10 个函数修改/新增 |
| `src/services/sceneService.ts` | 修改 | 5 个函数修改/新增 |
| `src/services/versionService.ts` | 修改 | `rollbackToVersion` 校验 prompt 非软删除 |
| `src/store/useAppStore.ts` | 修改 | 新增 trashCount + 8 个 actions |
| `src/pages/HomePage.tsx` | 修改 | 删除确认文案改为"移入回收站" |
| `src/components/layout/Sidebar.tsx` | 修改 | 新增回收站入口 + promptCount 加过滤 |
| `src/components/layout/StatusBar.tsx` | 修改 | promptCount 加过滤 |
| `src/pages/TrashPage.tsx` | **新建** | 回收站页面（列表 + 恢复/删除 + 空状态） |
| `src/components/trash/TrashItem.tsx` | **新建** | 回收站单项组件 |
| `src/App.tsx` | 修改 | 新增 `#/trash` 路由 |
| `src/utils/export-import.ts` | 修改 | 导出加过滤 + 导入剥离 deletedAt |

---

## 九、二轮评审补充修正

### 9.1 restoreScene 独立删除处理

用户可能先独立删 prompt 再删 scene。恢复 scene 时，对已被独立删除的 prompt 应保持删除状态：

```ts
export async function restoreScene(id: string): Promise<void> {
  await db.transaction('rw', db.scenes, db.prompts, async () => {
    await db.scenes.update(id, { deletedAt: undefined });
    // 仅恢复与场景同时被删除的 prompt（同一时间戳±1s内），独立删除的保留
    const sceneDeletedAt = (await db.scenes.get(id))?.deletedAt ?? Date.now();
    const prompts = await db.prompts.where('sceneId').equals(id).toArray();
    for (const p of prompts) {
      if (p.deletedAt && Math.abs(p.deletedAt - sceneDeletedAt!) < 2000) {
        await db.prompts.update(p.id, { deletedAt: undefined });
      }
    }
  });
}
```

### 9.2 Toast 系统增强

`use-toast.ts` 当前仅支持 `title + description`。需要新增：
- `action?: { label: string; onClick: () => void }` 支持撤销按钮
- `duration?: number` 自定义倒计时（默认 5000ms）

```ts
toast({
  title: '已移入回收站',
  action: { label: '撤销', onClick: () => restorePrompt(id) },
});
```

### 9.3 版本回滚行为

版本回滚不自动恢复已删除 prompt。抛错阻止：

```ts
// versionService.ts rollbackToVersion()
const prompt = await db.prompts.get(promptId);
if (!prompt) throw new Error('提示词不存在');
if (prompt.deletedAt != null) throw new Error('提示词在回收站中，请先恢复');
```

### 9.4 函数命名迁移

现有 `deleteScene`/`deletePrompt` 保留为硬删除（内部使用）。新增 `trashScene`/`trashPrompt` 为用户触发软删除。HomePage 改用 store action：

```ts
// 之前：await deleteScene(id)
// 之后：await trashScene(id)  // 软删除 + Toast
```

### 9.5 文件路径修正

`activeOnly()` 放入 `src/utils/helpers.ts`（非 `src/services/helpers.ts`）。

### 9.6 审计表补充

`exportPrompt()` 也需加 `deletedAt` 过滤（和 exportScene 同理）。

### 9.7 E2E 测试影响

| 文件 | 影响测试 | 修复 |
|------|---------|------|
| `regression.spec.ts` | "删除空场景" — 确认对话框变 Toast | 修改测试匹配 Toast 文案 |
| `phase2.spec.ts` | "场景CRUD" Delete 步骤 | 同上 |
| 新增 | 软删除7场景 + undo 恢复 + 回收站列表 | `trash.spec.ts` |

---

## 十一、投入/收益/风险

| 维度 | 评估 |
|------|------|
| **投入** | 中（约 5-7h），14 个文件 |
| **收益** | 高 — 防止误删数据丢失，用户安全感提升 |
| **影响** | 中 — 11 个查询点需加过滤，遗漏一处即显示异常 |
| **风险** | 中 — 软删除后存储量增加，30 天清理兜底 |
| **防御措施** | `activeOnly()` 辅助函数 + 单元测试 + dev 模式断言 |

### 一轮 + 二轮评审修正汇总

| # | 问题 | 处理 |
|---|------|------|
| 1 | 遗漏 6 个查询点 | 已补充到 §3 完整审计表 |
| 2 | 场景级联状态叠加 | `trashScene` 仅标记活跃 prompt（§4.2） |
| 3 | 恢复 prompt 孤儿 | 弹窗选择目标场景（§5.5） |
| 4 | 导入不剥离 deletedAt | `importData()` 强制清除（§4.4） |
| 5 | 缺少 Undo Toast | 已加入 §5.1 |
| 6 | 分阶段建议 | 同步上线场景+提示词回收站 |
| 7 | trashPrompt 无幂等检查 | §4.1 加 null-check（二轮） |
| 8 | savePrompt 不自动恢复 | update 分支显式设 `deletedAt: undefined`（二轮） |
| 9 | emptyTrash N+1 事务 | 合并为单批事务（二轮） |
| 10 | restoreScene 恢复独立删除项 | 时间戳比对，仅恢复同时删除的（二轮） |
| 11 | Toast 缺交互能力 | 增强 toast 支持 action button（二轮） |
| 12 | 版本回滚行为未定义 | 抛错阻止（二轮） |
| 13 | exportPrompt 审计遗漏 | 补充过滤（二轮） |
| 14 | helpers.ts 路径错误 | 改为 utils/helpers.ts（二轮） |
| 15 | deleteScene/trashScene 命名迁移 | 保留旧函数为硬删除，新增为软删除（二轮） |
